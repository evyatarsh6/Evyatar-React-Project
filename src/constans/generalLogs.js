export const generateChangeValueLogs = (elem, value ) => `${elem}` + ' has change and now has the value:' + `${value}`

export const generateUpdateCardLogs = (elem) => 'the following TODO ' + `${elem.kind}` + ' with the ID of ' + `${elem.kind}` + '  has been updated'